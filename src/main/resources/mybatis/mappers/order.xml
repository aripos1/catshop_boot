<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="order">

	<insert id="insertReceipt" parameterType="ReceiptVo">
		INSERT INTO receipt (no, user_no, express, address, total_price, payment_date, payment )
		VALUES (null, #{userNo}, #{express}, #{address}, #{totalPrice}, #{paymentDate}, #{payment})
	</insert>

	<insert id="insertItem" parameterType="ItemVo">
		INSERT INTO item (no, amount, each_price, taste, options_no, receipt_no,)
		VALUES (null, #{count}, #{eachPrice}, #{taste}, #{optionsNo}, #{receiptNo})
	</insert>




</mapper>